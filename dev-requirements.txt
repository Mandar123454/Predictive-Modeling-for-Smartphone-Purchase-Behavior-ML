# Developer and test-only dependencies (not required on Azure App Service)
# Install locally with: pip install -r dev-requirements.txt

# Testing
pytest==7.4.0
pytest-flask==1.2.0

# Optional plotting (for local notebooks or demos)
plotly==5.15.0
# Dash was previously included but not used by the deployed app
# If you experiment with Dash locally, uncomment the next line and ensure Flask <2.3
# dash==2.12.1

# Legacy/backport utilities (not needed on Python 3.11+)
# pathlib2==2.3.7
# pickle-mixin==1.0.2
